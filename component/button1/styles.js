import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        paddingVertical: 15,
        paddingHorizontal: 8,
        borderRadius: 8,
        width: '100%',
        backgroundColor: '#4F63AC',
    },
    text: {
        fontSize: 16,
        color: 'white',
        fontWeight: "bold",
        textAlign: 'center',
        marginTop: 10,
    }
});